spring:
  datasource:
    url: ${url}
    username: ${db_user}
    password: ${db_pass}

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

congress.api.key: ${api_key}
congress.api.url: "https://api.congress.gov/v3/"